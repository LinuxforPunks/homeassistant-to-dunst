alias: DINNER TIME!
description: this triggers the shell script on the PCs. The shell script plays the audio file, but the alert message is defined here
triggers:
  - entity_id: input_button.test_alert_button
    trigger: state
actions:
  - action: mqtt.publish
    data:
      topic: desktop/gaming/alerts
      payload: DINNER TIME!

